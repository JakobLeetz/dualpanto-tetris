/// <summary>
/// Used by both GridManager (falling piece logic) and LevelDefinition (allowed pieces per level),
/// so it gets its own file per the project's enum-extraction rule.
/// </summary>
public enum PieceType
{
    I // 4x1 piece, the only shape needed for Level 0/1
}
